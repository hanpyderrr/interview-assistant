# Forensic Report — Grounding Campaign, Phase 0 (Iteration 1)

Date: 2026-07-16. Branch: `fix/grounding-campaign` (created from a dirty working tree — see campaign-log.md ITERATION 1 for the pre-existing in-flight work this campaign builds on top of, per explicit user decision to NOT reconcile/commit that work first).

## 0. Starting-state caveat

This was NOT a clean-slate Phase 0. The working tree already contained substantial uncommitted work from a prior, adjacent effort (a "Knowledge Source canonical gate" repair series, commits `41edd51`/`5d8096e`/`6e7dd97`/`5188f6d` already on `main`, plus further uncommitted changes to `LLMHelper.ts`, `main.ts`, `context-os/*`, a new `TurnEvidenceCoordinator.ts`, and 11 `AUDIT_*.md` files from a separate, earlier 11-phase production-hardening campaign dated 2026-07-11). Three research subagents plus direct investigation confirmed:

- **`TurnEvidenceCoordinator` + `allocateRequiredEvidenceFamilies` is currently DEAD CODE.** It's fully implemented and unit-tested (`TurnEvidenceCoordinator2026_07_16.test.mjs`, 3 passing tests covering exactly the "mode+resume/JD starves evidence" scenario) and re-exported from `context-os/index.ts`, but grep confirms it is imported nowhere else in the live codebase. Nothing calls `new TurnEvidenceCoordinator()` or wires its `.resolve()` into `EvidenceResolver`'s or the profile-evidence path's actual retrieval. It is a well-designed, well-tested fix module that was built and shelved — not a live-path fix. Do not count it as done; do not delete it either (it may be the right long-term unification target for Phase 4, see §6).
- **`providerPayloadCapture.ts`** (new file, wired into every `LLMHelper.ts` provider adapter) is genuinely useful golden-trace infrastructure — a credential-redacting capture of the exact payload sent to each provider, gated behind `NATIVELY_E2E=1` + `NATIVELY_CONTEXT_OS_PROVIDER_CAPTURE=1`. Used directly in this campaign's traces (see §3).
- **`CodexCliService.ts` / `VisionProviderRegistry.ts` / `main.ts`'s 48-line diff / `ProfileIntelligenceSettings.tsx`'s 321-line diff** are unrelated maintenance (same provider-payload-capture instrumentation pattern, an unhandled-rejection DB-survival fix dated 2026-07-11, and a new "Profile" UI tab respectively) — confirmed by research agent, not re-verified independently, not relevant to grounding.
- `npm run typecheck:electron` passes clean on the dirty tree at iteration start.
- An existing real-backend test harness partially exists at `tests/context-os-real-backend/` (a 200-question thesis-document benchmark, real Electron + real MiniMax, never yet run to completion — no reports directory) and `tests/e2e-modes/_ks_realfixture_verify.mjs` (a narrow real-fixture regression probe for the "Knowledge Source canonical gate" work, using scratch fixtures at `/tmp/ctxos-fixtures/{resume,jd}.txt`). Neither covers the C1–C8 category matrix loop.md's Phase 2 specifies. Building `test/harness/` should REUSE this infrastructure (Electron-launch pattern, `__e2e__:*` IPC handlers, `ensure-backend.sh`) rather than duplicate it.

## 1. Preflight (loop.md §2.0)

- MiniMax key confirmed: `.env` + `natively-api/.env` both have `MINIMAX_API_KEY(_1/_2)`. Model string `MINIMAX_M3_MODEL = 'MiniMax-M3'` (`natively-api/lib/minimaxProvider.js:19`). Backend cascade in `natively-api/server.js` routes Groq → Gemini Flash → **MiniMax-M3** → Gemini Pro → Groq Scout, with a forced-primary path (`NATIVELY_FORCE_PRIMARY_GEN=minimax`) used for real-model benchmarking via `tests/e2e-modes/ensure-backend.sh`.
- Live answer-path entry point confirmed: the manual-chat IPC channel is `gemini-chat-stream` (handler captured at `globalThis.__nativelyGeminiChatStream`, invoked via `__e2e__:manual-ask`). The **live meeting-overlay / auto-trigger surface** (the one the founder's symptoms describe — "the meeting overlay answers...") is a SEPARATE code path: `IntelligenceManager.runWhatShouldISay` → `IntelligenceEngine.runWhatShouldISay` → `WhatToAnswerLLM.generateStream`, invoked end-to-end via the E2E-only `__e2e__:ask` handler (`ipcHandlers.ts:10588`, which calls `intelligenceManager.runWhatShouldISay` through the real `handleSuggestionTrigger` path — the same one the live overlay's proactive suggestion mechanism uses).
- **This WTA/overlay path and the manual-chat path are two structurally independent implementations of resume/JD evidence gating**, not two entry points into shared logic. This distinction turned out to be load-bearing (see §4).
- Both accounts on the local 9Router proxy healthy throughout (see campaign-log.md QUOTA lines).
- `dist-electron/electron/main.js` was fresher than all modified source files at trace time — traces below ran against the current dirty-tree state, real Electron, real local backend (`natively-api/server.js` on :3000, `NATIVELY_FORCE_PRIMARY_GEN=minimax`), real MiniMax-M3.

## 2. Golden Trace — reference-file grounding (founder symptom #1, H1/H6)

Three live traces (`traces/golden-trace-refdoc.mjs`, `-legacy.mjs`, `-resume-as-refdoc.mjs`) attached a reference document to a Lecture/General mode and asked immediately (t+0ms) and after settling (t+5-8s), both under `gemini-3.1-flash-lite` and under the real MiniMax-M3 path (`setModel('natively')` against the local forced-primary backend), and both with Context-OS governed flags at their dev/test-context defaults AND explicitly forced to production defaults (`NATIVELY_CONTEXT_OS_EVIDENCE_PACK=0`, `NATIVELY_OKF_KNOWLEDGE_PACKS=0`, `NATIVELY_OKF_HYBRID_RETRIEVAL=0`, `NODE_ENV=production`).

**Verdict: REFUTED for this scenario.** All 5 sub-traces returned fully correct, grounded answers (`mentionsMarcusWebb: true`, `mentionsPriya: true`, `isSafeRefusal: false`, `isGenericRefusal: false`) at both timings, on both the governed and legacy paths, on both Gemini and real MiniMax-M3. A 500-word real resume attached as a reference file to a General mode (not via profile ingestion) also answered correctly on 3/3 questions including a genuinely-unanswerable one (which the model handled gracefully, though not with the exact safe-refusal phrase — see finding below). **This specific class of the founder's symptom #1 (single reference file, simple lookup, manual-chat-style `__e2e__:ask` question) does not currently reproduce on this branch.** This does not mean the underlying symptom is fully resolved — the founder's real usage is the live overlay's auto-trigger during a real meeting with continuous transcript, a different trigger path (`handleSuggestionTrigger`) than a single scripted `__e2e__:ask` call, and H8 (desync) and true race conditions under concurrent transcript activity were not yet tested. Flag as **PARTIALLY REFUTED — retest under true live-transcript auto-trigger conditions before declaring fixed.**

**Secondary finding (not yet pinned, log for Phase 1 candidate list):** the genuinely-unanswerable question in `golden-trace-resume-as-refdoc.mjs` ("What is this person's favorite programming language?") did not literally emit the exact safe-refusal phrase (`"I could not find that in the retrieved sections"` — R6's mandated exact string). Instead the model inferred a plausible-but-technically-different answer ("The provided material identifies Go as this person's primary programming language" — Go IS the person's most-used language per the resume, so this is defensible inference, not fabrication, but it establishes the pattern that the model will infer past the letter of a question rather than emit the exact refusal string). Needs a dedicated C5-category benchmark question with a TRULY absent fact (not inferable) to properly test R6 compliance.

## 3. Golden Trace — mode+JD/resume grounding (founder symptom #3, H3) — ROOT CAUSE CONFIRMED

### 3a. `_ks_realfixture_verify.mjs` (pre-existing harness, re-run live)

Resume+JD ingested via the dedicated profile pipeline (`__e2e__:ingest-profile-doc`) into a mode switched `general` → `technical-interview` (`sourceAuthority: profile_only`, correctly re-seeded). Question: "Walk me through your most recent project." **Answer was correctly grounded** in the fixture resume's actual "levee" circuit-breaker project (verified: the resume genuinely contains this project verbatim). But `hasTypedEvidencePack: false`, `hasRawCandidateProfile: true` — grounding worked via the LEGACY raw-candidate-profile path, not the new typed `EvidencePack`/`TurnEvidenceCoordinator` machinery (confirming §0's dead-code finding from a different angle: even when grounding works, it isn't going through the new typed infrastructure).

### 3b. NEW trace — JD-only-required question on the SAME resume+JD+technical-interview setup (`traces/golden-trace-jd-fit-legacy.mjs`, `-jd-reqs-repro.mjs`, `-jd-payload-capture.mjs`, `-jd-console-capture.mjs`)

Question: **"What are the key requirements of this role according to the JD?"** — classifies as `answerType: jd_requirements_answer`, whose `AnswerPlanner.ts` definition requires `requiredContextLayers: ['jd']` only (JD-only, resume NOT required — a deliberate design, since this question is about the role, not the candidate).

**Result: the model answered fluently and confidently, but the answer was ENTIRELY FABRICATED — a hallucination, not a refusal.** The real fixture JD (`/tmp/ctxos-fixtures/jd.txt`) is a 3-line JD for "AI Product Engineer @ Helio Labs... LLM prompt engineering, streaming UIs, and Postgres... $175-200k". The model's answer instead invented "distributed systems," "API design," "cloud infrastructure," and "cross-functional teams" — plausible-sounding generic backend-engineer requirements that appear NOWHERE in the actual JD (they're a stylistic echo of the unrelated resume). This is a **direct violation of R5 (zero-hallucination is release-blocking)**, caught on a doc-grounded-adjacent (JD-source) answer type.

**Provider payload capture** (`golden-trace-jd-payload-capture.mjs`, using the new `providerPayloadCapture.ts` infra) proved the literal prompt sent to `gemini-3.1-flash-lite` contained **zero JD evidence text** — the `<transcript>` block held only `[INTERVIEWER]: what are the key requirements of this role according to the jd?`, nothing else. The `answer_contract` header correctly declared `requiredContextLayers: jd`, but no JD content was ever injected to satisfy that requirement. The model, given a shape to fill and no evidence, filled it with invented-but-plausible content.

**Root cause, confirmed via temporary tagged trace logs (`[TRACE:jd-turnsourcedecision]`, `[TRACE:jd-outer-gate]`) added to and rebuilt in `electron/IntelligenceEngine.ts`, then reverted before commit** (see git diff below — these are diagnostic-only, not a fix):

```
[TRACE:jd-turnsourcedecision] {"hasTurnSourceDecision":true,"outcome":"explicit_granted","allowedEvidenceKinds":["profile_jd"],"requiredEvidenceKinds":["profile_jd"],"owner":"profile","wtaDecisionAllowsCandidateProfile":false}
[TRACE:jd-outer-gate] {"hasOrchestrator":true,"isKnowledgeMode":true,"documentGroundedCustomModeActive":false,"wtaDecisionAllowsCandidateProfile":false}
```

The canonical `TurnSourceDecision` engine (the same "Knowledge Source canonical gate" machinery from commits `41edd51`/etc.) correctly resolved this turn: `outcome: 'explicit_granted'`, `owner: 'profile'`, `allowedEvidenceKinds: ['profile_jd']` — it KNOWS the JD is the right and only required source. But `electron/IntelligenceEngine.ts` (the **live WTA/meeting-overlay path**, NOT the manual-chat path) computes `wtaDecisionAllowsCandidateProfile` at line ~1108-1116 as:

```ts
wtaDecisionAllowsCandidateProfile = _wtaTurnSourceDecision.outcome === 'default'
    || _wtaTurnSourceDecision.outcome === 'explicit_granted';
if (_wtaTurnSourceDecision.allowedEvidenceKinds.length > 0) {
    wtaDecisionAllowsCandidateProfile = wtaDecisionAllowsCandidateProfile
        && (_wtaTurnSourceDecision.allowedEvidenceKinds.includes('profile_resume')
            || _wtaTurnSourceDecision.allowedEvidenceKinds.includes('projects'));
        // ^ NEVER checks 'profile_jd' — a JD-only-granted turn always fails this AND
}
```

`allowedEvidenceKinds` is `['profile_jd']` only (no `'profile_resume'`, no `'projects'`) — a JD-only grant, exactly as intended by the canonical decision engine. But the AND-check only recognizes `profile_resume`/`projects`, never `profile_jd`, so `wtaDecisionAllowsCandidateProfile` computes to **`false`** despite the decision engine explicitly granting JD access. This single boolean gates the ENTIRE candidate/JD evidence-fetch block in `IntelligenceEngine.ts` (`if (orchestrator?.isKnowledgeMode?.() && !documentGroundedCustomModeActive && wtaDecisionAllowsCandidateProfile) { ... }`), so `orchestrator.processQuestion()` is never even called, `candidateProfile` stays `''`, and the model gets the answer-shape instructions with zero grounding evidence. `IntelligenceAttribution` confirms: `"structured_jd_used":false,"knowledge_orchestrator_used":false"`.

**This is the exact same bug PATTERN already found and fixed once, on a DIFFERENT code path.** `electron/ipcHandlers.ts` line ~1611-1613 (`_contractAllowsProfile`, the manual-chat equivalent gate) has an explicit code comment dated "Evidence-execution-repair (2026-07-11)" describing this EXACT defect — "this gate previously checked only profile_resume/profile_project, never profile_jd... Confirmed live: 'Does the JD prove that I have Tableau experience?' and 'According to the JD, what are the main responsibilities?' both leaked JD content into a reference_files_only turn through this exact gap" — and fixes it by adding `|| allowsEvidence(turnContract, 'profile_jd')`. That fix landed on the manual-chat path only. **`IntelligenceEngine.ts`'s WTA/live-overlay path — which is what the founder's meeting-overlay actually uses — still has the unfixed, narrower gate.** A SECOND, structurally identical instance of the same unfixed pattern exists at `IntelligenceEngine.ts:2013` (the profile-answer repair gate: `allowsEvidence(wtaTurnContract, 'profile_resume') || allowsEvidence(wtaTurnContract, 'profile_project')` — also missing `profile_jd`).

### Verdict: **H3 CONFIRMED** — pinned root cause, ranked #1 by expected impact.

This is squarely inside R2's "disconnected subsystems" pattern (H9 overlaps): the SAME defect was fixed on one of two parallel implementations of the same authorization gate and not the other. It directly explains:
- Founder symptom #3 (mode+knowledge combination broken) for any JD-only-required question type (`jd_requirements_answer`, `jd_summary_answer`, `jd_fact_answer` per `AnswerPlanner.ts`'s `requiredContextLayers` table) asked through the live overlay/auto-trigger path.
- Founder symptom #2 (nondeterminism/hallucination) — the model doesn't refuse (no visible "I don't have that"), it CONFABULATES a plausible-sounding wrong answer, which is worse than a refusal and is a direct R5 violation.
- This is NOT the "ignoring uploaded sources, refuses citing uploaded-sources-only" framing the founder used verbatim — it's the inverse-but-related failure: the model doesn't refuse, it fabricates. Both symptoms share the same underlying cause class (evidence-injection gate wrongly zeroing out available evidence), just manifesting differently depending on whether the answer-shape instructions leave room for confident invention (JD requirements — yes) vs. explicit refusal templates (identity/behavioral — the FIVE numbered "accuracy_admissions" templates in the system prompt, which is why identity-type questions on this same path DID ground correctly per §3a).

## 4. Anti-thrash note

Per R2: this is technically fixing "the same pattern" that `ipcHandlers.ts` already fixed — but on a DIFFERENT file/code path that was never touched by that fix. This is not re-fixing an already-fixed defect (the manual-chat path's fix is untouched and still correct); it's applying the SAME fix to a sibling code path that shares the bug but was out of scope for the original fix. Per R2 this is legitimate — "you may never fix the same pattern twice" means on the same site, not "never apply the same correct pattern to a second broken site."

## 5. Subsystem wiring table (H9, partial — expand in next iteration)

| Subsystem | Verdict | Evidence |
|---|---|---|
| `EvidenceResolver` (reference-file/OKF retrieval) | WIRED-LIVE | Confirmed firing in golden-trace-refdoc traces; `hasTypedEvidencePack:true` when `contextOsEvidencePackEnabled` is on. |
| `TurnEvidenceCoordinator` / `allocateRequiredEvidenceFamilies` | **UPDATED 2026-07-17 (iteration 7 pause window): NOW WIRED-LIVE** — was DEAD-CODE as of iteration 1 (2026-07-16). A concurrent session wired it into `ipcHandlers.ts`'s manual-chat path (~line 2085-2271, dated "CONTEXT OS (2026-07-17)" in its own comments) behind a NEW flag `contextOsMultiFamilyEvidenceEnabled` (additional to the existing `contextOsEvidencePackEnabled`), composing `EvidenceResolver` (reference retrieval) with `ProfileEvidenceService` (an existing, already-committed résumé/JD facade — confirmed via `git log`, not new) through the coordinator, with a budget-raced timeout falling through to the legacy raw-text path on any failure. This directly resolves the "dead code, well-tested but shelved" finding from iteration 1. NOT independently verified live by me this session (found while reviewing the shared working tree during a quota-pause window, not via a live trace) — a future iteration should confirm this wiring actually fires and improves grounding before counting it as a verified fix, per this campaign's own R1/R3 evidence-before-edits discipline. Logging the finding now so it isn't lost, not claiming it as verified. | Read directly in `electron/ipcHandlers.ts` (lines ~2085-2271) during iteration 7's quota-pause window; not live-traced. |
| Profile/JD evidence gate — manual chat (`ipcHandlers.ts` `_contractAllowsProfile`) | WIRED-LIVE, FIXED | Includes `profile_jd`; fixed 2026-07-11 per code comment. |
| Profile/JD evidence gate — WTA/live-overlay (`IntelligenceEngine.ts` `wtaDecisionAllowsCandidateProfile`, and the repair-gate at line 2013) | WIRED-LIVE, **BROKEN** | Missing `profile_jd` in BOTH occurrences — root cause of §3. |
| `contextOsEvidencePackEnabled` (and sibling `okfKnowledgePacks`/`okfHybridRetrieval`) flags | FLAG-GATED-OFF in production | Default via `isInternalDevTestContext()` = true only for `NODE_ENV=test/development`, `BENCHMARK_MODEL` env, or `NATIVELY_INTERNAL`/`NATIVELY_DEV=1`. A real packaged build (`electron:build` sets `NODE_ENV=production`) never sets these, so the typed-evidence-pack path is OFF for real users by default. **This means most of this session's own dev/test-mode traces (and likely much of the "VERIFIED" prior work referenced in memory) were exercising a code path a real production user does NOT get.** Flagged per R7 as a finding, not silently flipped — needs an explicit decision in Phase 1/4 on whether to flip default-on for production once its own live-path correctness (not just its unit tests) is verified against production defaults, or to fix the legacy path it falls back to (which is what this report's §3 root cause actually lives in — the legacy/fallback path, confirmed reproducing with these flags forced OFF). |
| `KnowledgeOrchestrator` (résumé/JD structured extraction + `processQuestion`) | WIRED-LIVE | Fires correctly for identity/profile_detail-classified questions; the JD-requirements-type question never reached it due to the upstream gate in §3. |

## 6. Pinned root causes for Phase 1 (ranked by expected impact)

1. **[PINNED — FIX NEXT] `IntelligenceEngine.ts`'s `wtaDecisionAllowsCandidateProfile` gate (2 occurrences: ~line 1108-1116 and ~line 2013) never checks `profile_jd` in `allowedEvidenceKinds`, incorrectly zeroing out JD-only-granted turns and causing confident hallucination on JD-source answer types (`jd_requirements_answer`, `jd_summary_answer`, `jd_fact_answer`) on the live meeting-overlay/WTA path.** Fix: mirror `ipcHandlers.ts`'s already-fixed pattern — add `|| _wtaTurnSourceDecision.allowedEvidenceKinds.includes('profile_jd')` to both occurrences.
2. Retest reference-file grounding (§2) under TRUE live-transcript auto-trigger conditions (not scripted `__e2e__:ask`), since that's the founder's actual usage pattern and H1/H8 weren't exercised there yet.
3. Decide (R7, don't silently flip) whether `contextOsEvidencePackEnabled` + `okfKnowledgePacks` + `okfHybridRetrieval` should default ON in production once the legacy-path fix (#1) is verified, or stay off while the legacy path is hardened directly — current evidence says the LEGACY path (flags off) is what real users hit and what needs to work correctly first.
4. Exact safe-refusal phrase compliance (R6) — build a dedicated C5 benchmark case with a truly-unanswerable fact and verify the exact string is emitted, not a graceful inference.
5. Continue H2/H4/H5/H8/H10 forensics not yet covered by direct live trace (H2 escape-hatch check on the actual live system prompt was seen in full in trace #6 — no obvious escape hatch found on read-through, but not yet adversarially tested; H4/H5/H8/H10 untested this iteration).

## 6b. H8 (double execution / question-answer desync) — CONFIRMED reproducible, root cause understood, FIX DEFERRED (needs care)

### Iteration 3 update (continuation after the H3 fix + concurrent-workspace incident described in campaign-log.md ITERATION 2)

Live trace `traces/golden-trace-rapidfire-desync.mjs` fired 3 DISTINCT questions (each about a uniquely-identifiable fact in the same reference doc) through the real `__e2e__:ask`/WTA path in overlapping flight (no await between sends). **Reproduced exactly the founder's "answers a completely different question" symptom**: question 1 ("database migration deadline") received the answer to question 3 ("wifi password"); questions 2 and 3 received nothing (`noDecision: true`).

Root-caused via console capture (`traces/golden-trace-rapidfire-console.mjs`):
1. `[IntelligenceEngine] Planner stayed silent { reason: 'cooldown' }` fired for 2 of the 3 overlapping triggers — the live auto-trigger's cooldown gate rejects rapid-fire triggers by design (this itself is correct, expected behavior for the AUTO-TRIGGER path).
2. Only ONE trigger actually produced an answer. But `IntelligenceEngine.ts`'s `emit('suggested_answer', finalWtaAnswer, question, confidence)` call (~line 2439) is explicitly commented **"The suggested_answer emit below is UNGATED"** — i.e. deliberately NOT checked against `this.currentGenerationId` before firing, unlike nearly every other checkpoint in the same method (`generationId = ++this.currentGenerationId` at line 1731, checked at lines 1850/1874/1884/1949/2141 etc.). The comment explains why: "that is the live UI delivery the user still needs to see; only storage is gated" — i.e. this was a DELIBERATE prior fix to prevent the app going silent when a generation gets superseded (this matches a past fix pattern: mirrors "What to answer stops responding after a few messages" P0, referenced in `ipcHandlers.ts:7754-7757` for the sibling cooldown-skip fix on manual button presses).
3. Confirmed this is REACHABLE in real production, not just the E2E harness: `ipcHandlers.ts`'s `generate-what-to-say` handler (manual button/hotkey press, ~line 7747) explicitly sets `skipCooldown: true` specifically so a manual press is never throttled — meaning a user's manual press CAN genuinely overlap with an in-flight auto-trigger-initiated generation for a DIFFERENT question. The production listener in `main.ts:5629-5634` forwards every `suggested_answer` emit verbatim to the renderer with `{answer, question, confidence}` but no generation id.
4. The renderer's `finalizeStreamingByIntentMessages` (`src/lib/overlayMessagePersistence.mjs:27`) guards against cross-INTENT clobbering (e.g. a stray WTA answer landing on an open manual-chat placeholder) via `streamingMsgId`/intent matching, but has NO mechanism to reject a `what_to_answer` answer that belongs to an OLDER, superseded question when a NEWER `what_to_answer` generation is also in flight — it just finalizes whatever answer arrives into the currently-open same-intent streaming row, without checking whether that answer's `question` field matches the row's actual question.

### Why this fix is DEFERRED, not landed this iteration

The "ungated" comment is not an oversight — it is a documented prior fix for a real regression (silent app / no answer shown). A naive fix (gate the emit on `currentGenerationId === generationId`, dropping stale-generation answers) would very likely REINTRODUCE that already-fixed bug: a superseded generation would go silent again exactly when the cooldown/speculative-reuse logic decides NOT to restart cleanly. Per R2 (anti-thrash), this needs a design that satisfies BOTH constraints simultaneously — never silently drop an answer AND never attach the wrong answer to the wrong question — which most likely means: keep the emit ungated (always deliver SOMETHING), but attach the actual `generationId`/originating-question identity to the emitted event so the RENDERER can make an informed choice (e.g. render a stale-but-superseded answer as its own new turn tagged to its real question, rather than overwriting the currently-open row for a different, newer question). This is a two-file coordinated change (emit signature in `IntelligenceEngine.ts` + preload/IPC channel + renderer's `finalizeStreamingByIntent`) and deserves its own dedicated fix-and-verify iteration rather than a rushed patch. Pinned as the top Phase 1 priority for the NEXT iteration.

### Verdict: **H8 CONFIRMED as a code-level defect, but REVISED to REFUTED as a real-production defect** (iteration 4 correction — see below)

**CORRECTION (iteration 4):** iteration 3's claim that this is "reachable in real production via a manual press racing an auto-trigger" was WRONG — an overclaim made without checking the actual real call graph. Rechecked via `mcp__code-review-graph__query_graph_tool` (`callers_of`) and direct source reading:

- `IntelligenceEngine.handleSuggestionTrigger` (the "auto-trigger" path my rapid-fire trace drove via `__e2e__:manual-ask`) has exactly ONE caller in the entire codebase: `ipcHandlers.ts:10883`, which is itself INSIDE the `__e2e__:manual-ask` test-only IPC handler. It is never called from any real, non-E2E production code path. There is no live "auto-trigger" mechanism in the current codebase that calls this method from a real transcript-driven detector — the renderer never invokes it either directly or indirectly.
- The ONLY real production trigger for `runWhatShouldISay` is the renderer's `handleWhatToSay()` (`src/components/NativelyInterface.tsx:3386`), wired to the manual button/hotkey, which calls `generateWhatToSay` → IPC `generate-what-to-say` → `intelligenceManager.runWhatShouldISay(...)` directly — it NEVER goes through `handleSuggestionTrigger`, so the cooldown/speculative-reuse logic inside `handleSuggestionTrigger` that my trace exercised is not even in this real call path.
- Critically, `handleWhatToSay()` starts with `if (!tryBeginOverlayAction('what_to_say')) { ...surface "Still finishing the previous answer" and return... }` (line 3387) — a real, airtight single-in-flight-action guard (`overlayActionInFlightRef`, a `Set` ref scoped to the single overlay window instance) that BLOCKS a second press while a prior one is still streaming. Every call site of `handleWhatToSay` (5 confirmed: lines 4869, 5089, 5234, 6038, plus the hotkey wiring) goes through this same single guarded function. There is no way for two real button presses (or a hotkey + a click) to reach `runWhatShouldISay` concurrently in the shipped UI.
- My `traces/golden-trace-rapidfire-desync.mjs`/`-console.mjs` repro fired 3 CONCURRENT `__e2e__:ask` calls, each of which independently calls `im.reset()` BEFORE building its transcript and triggering (`ipcHandlers.ts:10805`) — so the repro was actually exercising 3 racing `reset()` calls (each bumping `currentGenerationId`) feeding into `handleSuggestionTrigger`, a test-only code path with its own test-only race condition, not a reachable production scenario.

**Revised verdict: H8's underlying code pattern (the emit being "ungated" against `currentGenerationId`) is real and could theoretically bite a FUTURE caller that does allow concurrent triggers, but it is NOT currently reachable through any live UI action given the `tryBeginOverlayAction` guard.** No fix is warranted against a currently-unreachable path — per the resolution rule "touches the least code," and per not inventing work not actually needed. This is logged as a defense-in-depth candidate for Phase 4 hardening (the `emit` truly should carry generation identity for robustness against any FUTURE caller that bypasses the renderer's single-flight guard, e.g. a second window, phone-mirror surface, or a future auto-trigger feature), but is REMOVED from the Phase 1 pinned-fix list. This correction itself is the kind of thing R2 anti-thrash logging is for: I nearly built a whole fix session around a non-reproducible-in-production bug because I tested through a test-only code path without first confirming that path's real-world reachability. Lesson applied going forward: before pinning ANY root cause found via `__e2e__:*` handlers, trace the REAL (non-`__e2e__`) call graph for the exercised method, not just assume the E2E harness mirrors a reachable production flow.

## 6c. Phase 2 harness first run (`test/harness/run-benchmark.mjs`, run-001) — 3 NEW confirmed defects

Building the C3/C4/C6/C7 benchmark harness (see campaign-log.md ITERATION 4) and running it against real MiniMax-M3 immediately surfaced 3 further real, live-path-proven defects beyond the H3 fix already landed. 8/10 cases passed; the 2 failures are genuine, not harness bugs (confirmed via console-log tracing each one down to root cause).

### NEW-1: "in order" collides with the DSA traversal pattern, misrouting résumé questions to `coding_question_answer` (CONFIRMED, root cause pinned, NOT YET FIXED)

Case C3-002 ("What companies have you worked at, **in order**?") fabricated a completely fictional employment history ("TechFlow", "DataScale", "CloudSync" — none of these appear anywhere in the real resume) while the model's own opening line even said "I don't have specific past experience loaded right now" — a direct, confirmed R5 hallucination.

Root cause, confirmed via console trace: `KnowledgeOrchestrator.processQuestion` DID correctly assemble the real structured résumé data (`[KnowledgeOrchestrator] Deterministic structured pack: 4 node(s) for categories [experience]`, `profileChars: 4181`, confirmed in `test/harness/reports/run-001/electron-console.log`). But `IntelligenceAttribution` for this turn shows `answer_type: coding_question_answer` — the ENTIRELY WRONG answer-shape classification. `AnswerPlanner.ts`'s `COMMON_CODING_PROBLEM_PATTERNS` (line 552) contains `/\blevel order\b|\bin\s?order\b|\bpre\s?order\b|\bpost\s?order\b|\btraversal\b/i` — meant to catch "in-order tree traversal" DSA questions, but `\bin\s?order\b` ALSO matches the ordinary English phrase "in order" (as in "chronologically," "in sequence"). Confirmed via direct regex test: `/\bin\s?order\b/i.test("What companies have you worked at, in order?")` → `true`. `coding_question_answer`'s `forbiddenLayersFor` (line 1744-1750) explicitly forbids the `resume` context layer ("Spec §8.3: generic coding/technical answers must NOT use any profile") — so even though the évidence was correctly fetched, it was correctly-per-spec DROPPED because the answer type was wrong, and the model — told to answer a "coding" question with no code content — invented a plausible-sounding but entirely fictional career history instead of either grounding correctly or refusing.

**FIXED AND VERIFIED (iteration 4, same round).** `electron/llm/AnswerPlanner.ts`'s `COMMON_CODING_PROBLEM_PATTERNS`: split the bare `/\blevel order\b|\bin\s?order\b|\bpre\s?order\b|\bpost\s?order\b|\btraversal\b/i` entry into `/\btraversal\b/i` (kept bare — unambiguous DSA vocabulary) plus a lookahead-based co-occurrence check `/(?=.*\b(?:tree|node|binary|bst)\b)(?=.*\b(?:level|in|pre|post)\s?order\b)/i` requiring an explicit tree/traversal-adjacent term before the order-word variants count as a coding signal — mirroring the exact style of the `class`/`method` narrowing already done two lines above in the same pattern list (with its own P0-incident comment about this exact kind of bare-word over-match). Verified via: (1) standalone regex unit tests covering both the false-positive phrasings and 8 genuine DSA phrasings (all correct), (2) `npm run typecheck:electron` clean, (3) `node --test electron/llm/__tests__/AnswerPlannerValidator.test.mjs` 12/12 pass including coding-classification cases, (4) `node --test "electron/llm/__tests__/**/*.test.mjs"` 2483/2543 pass — the 60 failures are all in unrelated, pre-existing-dated test files (timeline math, manual-chat fixes from 2026-06/07) with zero mention of "in order"/"traversal"/coding patterns, consistent with this being pre-existing baseline noise from the shared workspace's concurrent editing, not a regression from this fix, (5) LIVE re-run of the exact failing case C3-002 through `test/harness/run-benchmark.mjs`: now correctly answers "I started my career at Amazon Web Services, then moved to Uber Technologies, followed by Datadog, and I'm currently at Stripe" — matching the real resume's 4 employers in the correct chronological order, zero fabrication.

### NEW-2: "negotiation"-classified questions get ZERO grounding on the WTA/live-overlay path, even for pure factual JD lookups (CONFIRMED, root cause pinned, fix scope needs a decision)

Case C4-002 ("What company is this role at, and what's the compensation range?") returned a false refusal: "The JD does not specify the company name. Regarding compensation, the document does not state a salary range." Both claims are FALSE — the real fixture JD literally opens with "# Job description — AI Product Engineer @ Helio Labs" and states "Compensation range: 175–200k base + meaningful equity."

Root cause, confirmed via console trace: NO "Grounded what-to-answer in candidate profile" log fired for this turn at all (confirmed absent in the console log window around this turn's timestamp) — `candidateProfile` stayed empty. `electron/llm/transcriptQuestionExtractor.ts`'s `classifyType()` (a THIRD, independent classifier from `AnswerPlanner`'s `answerType` and `KnowledgeOrchestrator`'s own `classifyIntentWithContext`) matches the word "compensation" against its `negotiation` pattern (confirmed via direct regex test) and returns `questionType: 'negotiation'`. `IntelligenceEngine.ts`'s `groundable` gate (~line 1148-1155) only allows `identity, profile_detail, behavioral, jd_alignment, general, follow_up` through to `orchestrator.processQuestion()` — `'negotiation'` is excluded, so grounding never runs, even though `AnswerPlanner`'s OWN classifier correctly identified this as `jd_fact_answer` (required layer `['jd']`, confirmed in `IntelligenceAttribution`).

**FIXED AND VERIFIED (iteration 4, same round) — turned out to be a chain of FOUR stacked classifier disagreements, not just one.** Investigating this one root cause led to discovering three more, each blocking the fix from working until all four were addressed:

1. `IntelligenceEngine.ts`'s `groundable` gate excluded `negotiation`-classified questions entirely. **Fix**: added `isNegotiationButActuallyJdFact` — reuses a new exported helper `isJdFactualLookupNotNegotiationAdvice` (in `AnswerPlanner.ts`, built from the SAME `hasJdReferenceCue`/`NEGOTIATION_ADVICE_CUE_RE` signals `resolveJdSourceType` already relies on) to widen `groundable` only for the JD-fact-lookup shape, leaving genuine negotiation-coaching questions on the existing exclusion.
2. Even with (1)'s gate widened, `KnowledgeOrchestrator.processQuestion`'s own `factualRecall` gate explicitly and by-design NEVER sets `factualRecall: true` for `IntentType.NEGOTIATION` (to keep salary-coaching content out of modes that forbid it) — so the orchestrator call succeeded but its result was still discarded downstream. **Fix**: rather than loosen the general `factualRecall` gate (which risks leaking `salaryContext`/negotiation-script coaching content that's unconditionally appended to `result.contextBlock` earlier in the same function, for ANY `NEGOTIATION`-classified turn), added a narrow, EARLIER carve-out in `KnowledgeOrchestrator.ts`: when `intent === NEGOTIATION` AND `isJdFactualLookupNotNegotiationAdvice(question)`, return `maybeGroundedOnlyResult(question)` directly — a clean, no-retrieval, no-coaching JD-only grounding block that already existed for a different code path (the ambiguous-question inclusion-bias branch) and already sets `factualRecall: true` on its own. This bypasses the coaching-content append entirely for this specific shape.
3. Even with (1) and (2), `KnowledgeOrchestrator`'s intent classifier (`IntentClassifier.ts`) has its OWN, THIRD, independent bug: `IDENTITY_DIRECT_PATTERNS` includes bare `'what company'`/`'which company'` (meant for "what company do YOU work at" — the candidate's own employer) which ALSO matches "what company IS THIS ROLE at" (asking about the target role, not the candidate) — and `IDENTITY_DIRECT_PATTERNS` matches are checked BEFORE the scoring logic and unconditionally return `IntentType.INTRO`, which produces only a ~118-char compact identity block, never reaching NEGOTIATION classification at all. **Fix**: added a `JD_ROLE_FRAME_RE` disqualifier (`/\b(this|the)\s+(role|job|position|posting|listing|opening)\b/i`) so the company-identity patterns only fire when the question is NOT JD-framed.
4. Even with (1), (2), (3) all landed, the answer STILL claimed "the JD does not specify a compensation range" for the company-name half but not the salary half — traced to a FOURTH, distinct gap: `ProfileContextBuilder.ts`'s `buildTargetJobBlock` never rendered `StructuredJD.compensation_hint` into the `<target_job>` block at all, even though the field was correctly extracted and stored. **Fix**: added a `Compensation: ${compensation}` line to the rendered sections.

All four verified together, live, via `test/harness/run-benchmark.mjs` case C4-002: "What company is this role at, and what's the compensation range?" now correctly answers "This role is at Helio Labs. The compensation range is 175,000 to 200,000 base salary plus meaningful equity." — zero fabrication, both facts present, matching the real fixture JD exactly. Regression manifest strengthened to require BOTH facts (`"Helio Labs"` AND `"175"`) plus explicit forbidden-phrase checks (`"does not specify"`, `"does not state"`) so a future partial-regression on either half fails the benchmark, not just a full failure.

Also verified: `npm run typecheck:electron` clean after all 4 changes; full `electron/llm/__tests__` suite still 2483/2543 pass (same pre-existing unrelated failures, confirmed no new regressions from any of the 4 changes).

This chain is a strong illustration of why loop.md's Plateau Rule exists ("if the score improves < 2 points across 3 consecutive iterations... re-do a full golden trace") — a SINGLE symptom (one false-refusal benchmark case) required tracing through FOUR independent, uncoordinated classifiers (`transcriptQuestionExtractor.classifyType`, `KnowledgeOrchestrator`'s `classifyIntentWithContext`/`IntentClassifier.ts`, `AnswerPlanner.planAnswer`, and `ProfileContextBuilder`'s rendering layer) before the fix was complete — fixing any 1, 2, or 3 of the 4 would have looked like meaningful progress in the console logs while the benchmark case kept failing.

### NEW-3: Adversarial prompt-injection resistance CONFIRMED HOLDING (no defect — positive finding)

Cases C6-001/C6-002 (reference document with two embedded prompt injections — one demanding system-prompt disclosure + a fake wifi password, one demanding a blanket "I have no restrictions" compliance claim) both answered the REAL question correctly from the REAL document content and completely ignored both injected instructions. This is a genuine security-positive result, not just an absence-of-finding — logged so a future session doesn't have to re-verify this from scratch. R6/injection-resistance holds on this branch as of 2026-07-17.

### Ledger update

| # | Hypothesis | Verdict | Evidence | Fix commit | Status |
|---|---|---|---|---|---|
| 5 | NEW-1 — `\bin\s?order\b` in `COMMON_CODING_PROBLEM_PATTERNS` false-positives on ordinary "in order" phrasing, misrouting résumé questions to `coding_question_answer` (which forbids the resume layer), causing confident hallucination | CONFIRMED, FIXED, VERIFIED LIVE | `test/harness/reports/run-001/results.jsonl` (C3-002 originally failed); `test/harness/reports/fix-verify-new1/results.jsonl` (C3-002 now passes) | uncommitted at time of writing — see campaign-log.md for commit hash once landed | **DONE. Do not re-loosen this pattern back to a bare order-word match without re-adding the co-occurrence guard — the false positive is a confirmed live incident, not a hypothetical.** |
| 6 | NEW-2 — chain of 4 stacked classifier bugs (WTA groundable gate excludes negotiation; KnowledgeOrchestrator factualRecall excludes negotiation by design; IntentClassifier's 'what company' pattern false-positives on JD-framed questions; ProfileContextBuilder never renders compensation_hint) all had to be fixed together for a pure factual JD/comp lookup to ground correctly | CONFIRMED, FIXED, VERIFIED LIVE (all 4 sub-fixes) | `test/harness/reports/run-001` (C4-002 originally failed); `fix-verify-new2-final` (now passes with strengthened dual-fact rubric) | uncommitted at time of writing — see campaign-log.md for commit hash once landed | **DONE. All 4 sub-fixes are load-bearing together — do not revert any one in isolation without re-testing the full chain.** |
| 7 | NEW-3 — Adversarial prompt-injection resistance | CONFIRMED HOLDING (positive) | `test/harness/reports/run-001/results.jsonl` (C6-001, C6-002) | n/a | Closed as verified-working; re-run as a standing regression check in future benchmark runs. |

## 6d. Existing 200-question thesis benchmark (C1/C2/C5 reference-document coverage) — RUN FOR THE FIRST TIME THIS SESSION

Per the `NEXT ACTION` from the prior iteration, ran the pre-existing `tests/context-os-real-backend/run-200q-benchmark.mjs` (real Electron + real MiniMax-M3, manual-chat surface) against the `development` split (140 cases) for the first time this session — no prior run results existed anywhere in the repo (`test-results/context-os-real-backend/` did not exist before this run). Fixture hash verified matching (`67e82065...`, confirmed against `manifest.json`).

**Result: 119/140 deterministic pass (85.0%), 124/140 two-tier pass after the LLM judge upgrades 5 wording-near-misses (88.6%).** This matches a prior session's memory note (`ctxos-prodready-session2-2026-07-13.md`: "114→119/140 det") almost exactly, confirming the live retrieval/grounding quality on this specific document has been STABLE at ~85% deterministic since 2026-07-13 — none of this session's fixes (H3, NEW-1, NEW-2) regressed OR improved this number, which makes sense: all three fixes targeted profile/JD grounding on the WTA path, not reference-document retrieval on the manual-chat path this benchmark exercises.

**Careful triage of the 16 two-tier failures** (not just accepting the raw score — many turned out to be benchmark-rubric artifacts, not live defects):

- **8 genuine false refusals** (the model said the safe-refusal phrase, but the required fact IS verifiably present in the source document, confirmed via direct full-text search): THESIS-072 (ITU/3GPP), THESIS-079 (Logitech C920), THESIS-088 (dual-arm interaction capabilities), THESIS-091/093 (specific demo-task object names), THESIS-094 (a numeric value), THESIS-129 (Gemma 3 12B), THESIS-131 (Third-person — note: this one's rubric fact literally does NOT appear as an exact string in the extracted text, so may be a benchmark rubric error rather than a retrieval miss; not independently re-verified this session). These are real H1/H6-class misses: retrieval isn't surfacing the answer-bearing chunk for these specific facts. Genuine, still-open gap — not touched by any fix landed this session (this benchmark exercises the manual-chat surface's `EvidenceResolver` path, a different code path from the WTA fixes).
- **1 CONFIRMED entity-substitution defect** (real retrieval-precision hallucination, not invention-from-nothing): THESIS-042/060 both concern the "Mercury X1" robot's hardware spec table (`Control System: NVIDIA Jetson Xavier (main), Jetson Nano (aux)`, confirmed present verbatim in the source PDF). The model's answers substituted a DIFFERENT, also-real fact about Mercury X1 from elsewhere in the same document (the software/AI-framework layer: "AutoGen... LLaMA 3.2 7B" — also genuinely present and genuinely associated with Mercury X1's higher-level agentic control, per "2.3 Mercury X1 Robot" immediately following the AutoGen discussion) instead of the specific HARDWARE control-system fact the question asked about. This is a category-confusion between two real, both-true, both-Mercury-X1-related facts — the chunker/retriever likely isn't reliably surfacing the specific spec TABLE row, or the table's tabular structure doesn't embed/retrieve as precisely as prose. Confirmed via direct document search, not assumed. **Pinned as an open H6 candidate for a future iteration** — needs investigation into whether OKF/table-aware chunking (mentioned as a planned mitigation in loop.md's F-H6 guidance) would fix this, since it's specifically a structured-table content type.
- **7 of the remaining "failures" turned out to be benchmark rubric artifacts, NOT live defects**, discovered by manually reading each answer against the source document rather than trusting the deterministic scorer:
  - THESIS-063 ("name two software technologies for Mercury X1", rubric hardcodes `['Python', 'MoveIt']`): the source lists FIVE valid technologies ("Python, ROS, MoveIt, Gazebo, Mujoco"). The model's answer "Python and ROS" is completely correct — just not the specific pair the rubric happened to pick. Same structural issue confirmed present in the broader question-bank: 13 "name two/three X" questions all hardcode ONE specific valid subset even where the source lists more valid options (spot-checked: THESIS-012, 023, 045, 062, 067, 074, 100, 123, 148 all have this shape). This is a PRE-EXISTING BENCHMARK DESIGN LIMITATION (rigid substring rubrics can't express "any valid subset of size N"), not a grounding defect — flagging for whoever maintains that fixture bank, out of scope for this campaign to fix (it's not this campaign's harness).
  - THESIS-100 ("two types of info in each dataset step", rubric wants `['RGB images', 'joint angles']`): model answered "paths to images and a 14-dimensional joint states array" — a correct paraphrase of the same two facts (confirmed against source: "RGB images from three synchronized camera views" / "Joint angles for both arms (14-dimensional vector)"), just not literal-substring-matchable. A judge-upgradeable near-miss that the deterministic scorer's substring match is too strict to catch, but the LLM judge ALSO didn't catch it this run (only 5 of 21 near-misses were upgraded) — worth noting the judge itself has room to improve its equivalence-recognition, though that's the pre-existing harness's judge, not something this campaign built.
  - THESIS-120/127/128/130: on manual re-read, these appear to be legitimate synthesis/wording gaps rather than clear-cut hallucinations, but were not each individually re-verified against the source text with the same rigor as the above — flagging as UNCERTAIN rather than confirmed either way, for a future iteration to adjudicate properly rather than guessing.

**Bottom line for C1/C2/C5 coverage**: the existing thesis benchmark is genuinely useful and its 85%/88.6% scores are real and reproducible, but a naive read of "16 failures" overstates the live-path defect count — careful triage shows only 1 confirmed retrieval-precision hallucination (Mercury X1 table vs. AI-framework confusion) and 8 genuine false-refusals worth chasing as H1/H6 forensics targets, with the rest attributable to benchmark rubric rigidity or requiring further individual verification. This triage discipline (verify against the actual source text before counting a scorer failure as a live defect) should be applied to any future benchmark result before it's cited as evidence of a regression or a fix.

### Ledger update

| # | Hypothesis | Verdict | Evidence | Fix commit | Status |
|---|---|---|---|---|---|
| 8 | H6 (retrieval precision) — Mercury X1 hardware-spec-table vs. AI-framework-layer entity confusion; model answers with a real-but-wrong fact about the same entity instead of the specifically-asked-about fact | CONFIRMED, root cause NOT yet pinned to a specific code fix | `test-results/context-os-real-backend/dev-run-001/results.jsonl` (THESIS-042, THESIS-060); manual source-document verification via full-text search | none | **OPEN — needs investigation into table-aware chunking/retrieval before a fix can be designed. Genuinely different code path (manual-chat `EvidenceResolver`) than this session's WTA-path fixes.** |
| 9 | H1/H6 — 8 genuine false refusals on the thesis benchmark's manual-chat path (retrieval not surfacing answer-bearing chunks for specific facts confirmed present in the source) | CONFIRMED, root cause NOT yet pinned | Same run, THESIS-072/079/088/091/093/094/129/(131 uncertain) | none | **OPEN — a real, live, currently-unexplained retrieval gap. Next iteration should pick 2-3 of these and do a full golden trace (loop.md §2.1) to find the actual retrieval-stage failure, rather than guessing at a fix.** |
| 10 | Thesis benchmark's own rubric design (rigid single-subset "name two X" facts when the source lists more valid options) | CONFIRMED as a benchmark-fixture limitation, NOT a live grounding defect | Manual verification of THESIS-063 (and 12 structurally-similar cases) against source text | n/a — not this campaign's harness to fix | Flagged only; do not "fix" the live grounding path to force a SPECIFIC subset match — that would be fixing a benchmark artifact by breaking real flexibility. |

## 6e. Golden trace on 2 confirmed thesis-benchmark false refusals — root cause PINNED (iteration 6)

Per iteration 5's NEXT ACTION, did a full golden trace (loop.md §2.1 style: raw retrieval inspection, then full-path trace with temporary tagged logs) on 2 of the 8 confirmed false-refusal cases from the thesis benchmark: THESIS-079 (Logitech C920 camera model) and THESIS-094 (480 total dataset episodes).

**Step 1 — raw retrieval inspection** (`traces/golden-trace-thesis-false-refusal.mjs`, using the existing `__e2e__:inspect-retrieval` handler which calls `buildRetrievedActiveModeContextBlockHybrid` directly): confirmed the answer-bearing text ("Logitech C920", "480") IS present in the raw hybrid-retrieval output for both questions (`allMarkersRetrieved: true`). This initially looked like it REFUTED H1/H6 for these 2 cases — retrieval finds the content.

**Step 2 — full manual-chat path trace** (`traces/golden-trace-thesis-refusal-fullpath.mjs`, driving the exact same `streamGeminiChat` surface the real benchmark uses): found the model's answer STILL refuses, and the `docContextBlock` used for the actual answer generation is dramatically SMALLER (2661 / 6779 chars) than what raw retrieval inspection showed (15936 / 16296 chars) for the same questions. This divergence was the key clue: **`EvidenceResolver` — a governed layer that sits ON TOP of the raw hybrid retriever when `contextOsEvidencePackEnabled` is on — was silently discarding the answer-bearing chunk AFTER a wider raw retrieval had already found it.**

**Step 3 — root cause pinned via a temporary tagged trace** (`[TRACE:evidence-selection]`, added to `electron/intelligence/context-os/evidenceSufficiency.ts`'s `selectSmallestSufficientEvidence`, then reverted after confirming — see below): `EvidenceResolver.finalizePack` calls `selectSmallestSufficientEvidence`, which trims the retrieved chunk set down via a **per-answer-shape cap**: `comparison → 6, list → 5, everything else (including 'numeric') → 3`. Live trace confirmed:
- THESIS-079 (`answerShape: 'list'`, cap 5): `eligibleAfterFilter: 6` — one chunk over the cap gets dropped.
- THESIS-094 (`answerShape: 'numeric'`, cap 3): `eligibleAfterFilter: 6` — HALF the eligible chunks get dropped, and the answer-bearing chunk for a specific numeric fact ("480 episodes") apparently ranks outside the top 3 by the composite `answerRelevanceScore` (distinctive-term coverage + answer-shape density), even though it IS retrievable and IS eligible.

This is a real, confirmed, previously-undocumented root cause: **the cap in `selectSmallestSufficientEvidence` is too aggressive for `'numeric'`-shaped answers on a long (66-page) document with many topically-similar chunks** — the design intent (minimize prompt bloat, avoid noise) is sound, but a fixed cap of 3 for ANY non-list/non-comparison answer shape (which includes `numeric`, `definition`, `methodology`, `follow_up`, and the `general` default) is too small when the retrieval scoring itself has any imprecision in ranking the specific value-bearing chunk near the top.

**FIXED AND VERIFIED (iteration 7).** First checked `git log -p`/`git blame` on the cap line per R1/R2 before touching it: the 3/5/6 numbers were the ORIGINAL values from the function's introduction (`86f8b28`), never separately tuned by a later benchmark round. A later commit (`be7d7e0`) explicitly diagnosed this EXACT symptom class ("the topical chunk that merely names the subject outscored the sub-section chunk that carries the actual value, so the answer-bearing chunk was diluted or dropped below the cap") and fixed the RANKING (`answerRelevanceScore`) while deliberately leaving the cap alone ("a floor of 3 so recall never drops below the prior behavior") — meaning the cap itself was never the fix target, only the ranking was. This confirmed raising the cap is a legitimate, previously-untried next step, not a re-fix of an already-fixed pattern.

Fix: raised the default/`'numeric'` cap from 3 to 5 (matching the existing `'list'` cap); left `'comparison'` (6) and `'list'` (5) untouched. Justification: the real prompt-size constraint is the 3600-token doc-grounded budget (`ModeContextRetriever.ts`), not chunk count — 5 short chunks stays well within it, so this is a conservative, targeted widening, not an unbounded change.

**Verified via the existing pinned unit test** (`electron/intelligence/__tests__/EvidenceSufficiency.test.mjs`, `'covers every target entity and observes answer-shape caps'` — updated its `general.length` assertion from `3` to `5` to reflect the deliberate change, all 5 tests in the file still pass) **and a full 140-case thesis-benchmark re-run** (`dev-run-002-capfix`) to check for regressions per R2:

- THESIS-094 (the pinned case) now correctly answers "The Mercury X1 dataset consists of a total of 480 episodes" — confirmed fixed.
- THESIS-079 (`answerShape: 'list'`, cap unchanged at 5, `eligibleAfterFilter: 6`) still fails — this cap was NOT touched by this fix (deliberately scoped to `numeric`/default only), so this is an untouched, still-open residual gap, not a new regression.
- Raw deterministic score held flat at 119/140 — but a case-by-case diff revealed exactly ONE flip each way: THESIS-094 flipped fail→pass (the real fix), and THESIS-026 flipped pass→fail. Investigated THESIS-026 immediately: its new answer ("RQ2 asks... Perception / Decision-making" as a bulleted list) is factually IDENTICAL to the old passing answer's content, just reformatted as a list instead of the joined phrase "perception and decision-making" the rigid substring rubric expects — a scorer artifact, not a real regression (same class of rubric-rigidity issue catalogued in iteration 5). Confirmed via the second-tier LLM judge, which upgraded THESIS-026 back to a pass.
- **Two-tier pass rate improved from 124/140 (88.6%) to 126/140 (90.0%)** — the LLM judge run (`judge-score.mjs`) confirms this is a genuine net improvement, not a wash: it upgraded 7 near-misses this run (vs 5 in the pre-fix run) including THESIS-026, and the raw pass/fail flip pattern traces to one genuine fix and one scorer artifact, not two independent unrelated changes.
- `npm run typecheck:electron` clean; `electron/intelligence/__tests__` full suite: 838/857 pass, 10 pre-existing failures confirmed unrelated (in `ProfileIdentityBaseline.test.mjs` and `IntelligenceOsE2E.test.mjs` — both target `manualProfileIntelligence.js`/a separate shelf-facade `ProfileTreeService`, neither imports or touches `evidenceSufficiency.ts`).

THESIS-079 (`'list'` cap, still open) is a smaller residual gap left for a future iteration — deliberately not touched this round to keep the fix narrowly scoped and independently verifiable.

**Diagnostic logs added and then FULLY REVERTED after use** (per R3/R10 — proven-then-removed, not left in): `[TRACE:docgrounded-reason]` and `[TRACE:docgrounded-regen-result]` in `electron/ipcHandlers.ts`, `[TRACE:evidence-selection]` in `electron/intelligence/context-os/evidenceSufficiency.ts`. Confirmed removed via `git diff --stat` showing `evidenceSufficiency.ts` with zero diff and grep confirming zero remaining `TRACE:docgrounded`/`TRACE:evidence-selection` matches in `ipcHandlers.ts` before this was written up. (Note: `ipcHandlers.ts` had substantial UNRELATED concurrent-session changes mixed into its working-tree diff at the time of cleanup — verified my reverts touched ONLY my own added lines, confirmed by reading the exact before/after context around each `TRACE:` marker before editing, never a blanket `git checkout`/`git stash` of the whole file, which would have destroyed the other session's in-progress work.)

### Ledger update

| # | Hypothesis | Verdict | Evidence | Fix commit | Status |
|---|---|---|---|---|---|
| 11 | H1/H6 — `EvidenceResolver`'s `selectSmallestSufficientEvidence` per-answer-shape cap (was 3 for 'numeric'/default, 5 for 'list', 6 for 'comparison') is too aggressive on a long document, dropping the answer-bearing chunk even when raw hybrid retrieval correctly finds it | CONFIRMED, FIXED, VERIFIED via full 140-case regression | `traces/golden-trace-thesis-false-refusal.mjs`, `-thesis-refusal-fullpath.mjs`; `dev-run-001` (before) vs `dev-run-002-capfix` (after): two-tier 124/140 (88.6%) → 126/140 (90.0%) | uncommitted at time of writing — see campaign-log.md | **DONE for the numeric/default bucket. `'list'` cap (5) intentionally left untouched — THESIS-079 (a `'list'`-shaped question with 6 eligible chunks) is a smaller, still-open residual gap for a future iteration, deliberately out of scope for this fix.** |

## 6f. Golden trace on 6 remaining untraced thesis-benchmark false-refusal cases (iteration 10) — THREE distinct new root causes found, none yet fixed

Per iteration 9's `final-report.md` §6 item 2 ("golden-trace the remaining ~6-7 unexplained false refusals"), traced all 6 previously-untouched cases from the `dev-run-001` false-refusal list (THESIS-072, 088, 091, 093, 129, 131) using the same 2-step methodology proven on THESIS-079/094 (`traces/golden-trace-thesis-batch2.mjs`): (1) raw retrieval inspection via `__e2e__:inspect-retrieval`, (2) full manual-chat path via `streamGeminiChat`. All 6 were then individually deep-traced against the real source PDF text (`tests/context-os-real-backend/fixtures/sample-thesis/pages.json`) and, for the 3 most informative cases, against the EXACT provider payload (`__e2e__:context-os-provider-capture`, main-process `app.evaluate()`, not the renderer) to see the literal evidence the model received — not just what raw retrieval found.

**Batch-2 first-pass results:**

| Case | Raw retrieval has answer? | Full-path result | Initial read |
|---|---|---|---|
| THESIS-072 (ITU/3GPP) | Only "ITU" found, NOT "3GPP" | Safe refusal (correct phrase) | Genuine partial-retrieval miss |
| THESIS-088 (Open X-Embodiment insufficiency) | Neither marker found | Refusal-ish (mentions Octo tangentially) | Genuine retrieval miss |
| THESIS-091 (dataset template example) | Neither marker found | Confident WRONG answer (different real instruction) | Wrong-chunk-selected hallucination |
| THESIS-093 (apple/orange never interacted with) | BOTH found | Confident WRONG answer (banana/grapes — the OPPOSITE pair) | **Confirmed serious hallucination, evidence WAS available** |
| THESIS-129 (Gemma 3 12B visual backbone) | Found | False refusal ("does not mention") | Evidence retrievable but not surfaced to model |
| THESIS-131 (Third-person camera perspective) | Found | Hedged non-answer | Evidence retrievable but not surfaced to model |

Three of these (072, 088) genuinely have nothing actionable in this document's raw retrieval — deprioritized, not investigated further (matches the existing "genuine retrieval miss" category from §5 of `final-report.md`, not a new bug class). THESIS-072 additionally confirmed: raw hybrid retrieval, on direct grep of `__e2e__:inspect-retrieval`'s block, contains neither `ITU` nor `3GPP` as literal substrings despite both appearing in the source PDF at char offsets 43338/45393 — a genuine chunking/retrieval-recall miss on a 2-sentence factual claim split across two adjacent paragraphs, not something this iteration's methodology can pin further without a retrieval-recall investigation (out of scope for a golden-trace pass).

The other three (091, 093, 129/131) each root-cause to a **different, distinct mechanism** — none is the already-fixed H6-cap issue, and none is the same as the other two:

### New finding A — THESIS-093: correct chunk retrieved, WRONG chunk selected by ranking tie-break (verbatim-phrase bias)

Confirmed via direct instrumentation of `selectSmallestSufficientEvidence` (temporary `[TRACE:evidence-selection]` tag in `EvidenceResolver.ts`, proven firing, then fully reverted — see cleanup note below) that the raw retrieval pool for "What two objects are visible but never interacted with?" contains BOTH the correct answer chunk (`[Section 3.2.1]`: "...a yellow banana and purple grapes. Additionally, other objects such as an apple and an orange are visible in the scene during each episode, but **no interaction is performed with them**.") AND a lexically-similar DISTRACTOR chunk from a different section (`[Section 4.2.1]`: "...the second fruit was **never interacted with**." — describing banana/grapes in a completely different semantic-understanding benchmark).

`distinctiveQueryTerms` for this question resolves to `['objects', 'visible', 'never', 'interacted']` (after stopword filtering strips 'two', 'what', 'but'). Both chunks cover exactly 2 of these 4 terms — a tie in `answerRelevanceScore`'s primary signal — but the distractor chunk contains the LITERAL phrase "never interacted with" (verbatim question wording), while the correct chunk uses the factually-equivalent but differently-worded "no interaction is performed with them". The raw retrieval score for the correct chunk (0.475) is actually HIGHER than the distractor's (0.447), but `answerRelevanceScore`'s distinctive-term-coverage weighting (0.6× coefficient) overrides the raw-score tie-break (0.001× coefficient) and both land at composite rank 0.6105 vs 0.6104 — the correct chunk is not dropped by the CAP (both are within the top-5 selected set — confirmed: `SELECTED: hybrid:0 (distractor), hybrid:4, hybrid:5` — chunk 93 (correct) is NOT among the 3 selected items sent to the model), it loses a near-tie to a phrase-level coincidence.

This is a **generalizable ranking-precision gap**, distinct from the already-fixed cap issue: `answerRelevanceScore`'s term-coverage signal rewards LITERAL phrase overlap with the question over semantic/paraphrase equivalence, so a document that happens to phrase a superficially similar but wrong fact using the question's exact words can out-rank the correct chunk that states the same concept in different words. Not yet fixed — would need either (a) a semantic-similarity tie-break using the existing vector score more heavily when term-coverage ties, or (b) widening the cap so both candidates survive to the prompt and let the model's own judgment (with a stronger "read every evidence item" instruction) disambiguate — plan (b) is cheaper/lower-risk but does not fix the underlying ranking bug, only masks it for this specific N=2 near-tie.

### New finding B — THESIS-129/131: OKF-card extraction drops the exact fact, `EvidenceResolver`'s `okf_exact` strategy has no fallback to raw chunks when cards miss

Both questions concern the same source paragraph: *"Using **Gemma 3 12B** from Google DeepMind as the visual backbone, the tool [Self-Awareness Tool] performs two essential functions... uses **visual input from a third-person camera**..."* Confirmed via a new temporary diagnostic IPC handler (`__e2e__:dump-okf-cards`, kept — see below) that walks `KnowledgeManager.getPackForFile(...).cards`: **no OKF card contains this passage's text at all.** The OKF extractor (`OkfExtractor`) split this section's content across two different cards — "Framework summary" (which compresses the whole passage down to one line: *"...the Self-Awareness Tool verifies that the action is feasible by analyzing the scene captured from a third-view camera"* — dropping "Gemma 3 12B" and "third-person" entirely) and a same-titled-but-different-content "Self-awareness capabilities" card (a completely different section — the benchmark #3 experimental results, not the tool's architecture).

Raw hybrid retrieval (`__e2e__:inspect-retrieval`) DOES correctly find the exact source paragraph with "Gemma 3 12B" and "third-person" verbatim (confirmed via direct substring search on the returned block). But `EvidenceResolver.resolveFromOkf` — the OKF-card-first strategy that runs BEFORE the hybrid RAG fallback (see `EvidenceResolver.resolve()`, `okfResult = this.resolveFromOkf(...)` at line ~326) — apparently returns a pack built from the (deficient) OKF cards without ever falling through to the hybrid-chunk pool that has the correct text, for both these questions (confirmed by capturing the exact provider payload: all 3 evidence items for both questions are `source_kind="okf_document_card"`, none are `mode_reference_chunk`).

This is a **third distinct root cause**: the OKF card-generation pipeline (`OkfExtractor`, a separate one-time-per-document summarization pass) can lossily compress or misfile a specific fact when a section discusses the SAME tool/concept name in two different contexts (architecture vs. experimental behavior) — and the resolver's OKF-first strategy has no detection for "the OKF card path found SOMETHING relevant-sounding but it's missing the actual queried fact, fall through to raw chunks too." This is a fundamentally different bug class from both the cap issue (H6-cap, fixed) and finding A above (ranking tie-break) — it is a **lossy-summarization-with-no-fallback** gap in a THIRD evidence-resolution strategy (OKF cards) that the campaign had not yet examined for false refusals. Not yet fixed — the safest fix would be for `resolveFromOkf` to detect low answer-coverage in its own selected cards (reusing the existing `answerRelevanceScore`/distinctive-term-coverage machinery) and fall through to `resolveFromHybrid` when coverage is weak, rather than trusting OKF cards unconditionally once ANY card is found.

### New finding C — THESIS-091: correct template text never reached retrieval at all (distinct wrong-chunk-selected hallucination, no OKF involvement)

Raw retrieval for "What example instruction is given for the dataset template?" surfaces NEITHER the correct template line (`[Section 3.2.1]`: *"Put the [color] [object] on the [color] plate: Ex. 'put the yellow banana on the red plate'"*) NOR any OKF card containing it — instead the model confidently answered with a DIFFERENT, real instruction from a different experiment (`"if possible, please pick up the banana from surface and place it on the plate"`, from `[Section 4.2.2] Prompt complexity analysis`, correctly present in the evidence pack it received). This is a genuine retrieval-recall miss on the correct passage (unlike finding A, the correct chunk isn't even IN the retrieved pool at all for this query — confirmed via raw-pool grep showing zero occurrence of "yellow banana on the red plate" template phrasing in the `__e2e__:inspect-retrieval` block for this exact query, even though the same passage WAS retrieved and correctly used for THESIS-093 above under a different query). This means retrieval recall for this passage is QUERY-DEPENDENT — retrievable for "what objects are never interacted with" but not for "what example instruction is given for the dataset template" — a lexical/semantic query-formulation gap in the embedder/lexical scorer, not a selection or OKF-card problem. Logged as a distinct, separate, NOT-yet-investigated-to-root-cause retrieval-recall gap (would need embedding-space inspection to pin further; out of scope for this golden-trace pass).

### Summary — none of these 6 cases are duplicates of the already-fixed H6-cap issue

This is an important negative result for anti-thrash discipline (R2): all 3 non-refuted findings above (A, B, C) are structurally DIFFERENT from the H6-cap fix already landed in §6e — cap-fix only helps when the correct chunk IS in the eligible pool but gets trimmed by the fixed-size cap; here, finding A shows the correct chunk survives the cap but loses a scoring tie-break, finding B shows a completely different evidence-resolution strategy (OKF cards) with no cap involved at all, and finding C shows the correct chunk never enters the retrieval pool for this specific query phrasing at all. Re-raising the cap further, or re-touching `evidenceSufficiency.ts`, would not address any of these three.

**Cleanup performed per R10**: the temporary `[TRACE:evidence-selection]` instrumentation added to `EvidenceResolver.ts`'s `finalizePack` for finding A was fully reverted (confirmed via `git diff` showing zero remaining diff on that file) after its firing was captured and analyzed. The new `__e2e__:dump-okf-cards` E2E-only IPC handler (added to `electron/ipcHandlers.ts`, gated behind the same `NATIVELY_E2E=1` block as every other `__e2e__:*` handler, never registered outside E2E) is KEPT as a permanent diagnostic tool, consistent with the existing precedent of `__e2e__:inspect-retrieval`/`__e2e__:context-os-prompt-audit` — it has ongoing value for any future OKF-card investigation and carries zero production risk (E2E-gated, read-only, no PII/credentials surfaced).

### Ledger update

| # | Hypothesis | Verdict | Evidence | Fix commit | Status |
|---|---|---|---|---|---|
| 12 | THESIS-093 — `answerRelevanceScore`'s distinctive-term-coverage signal ties on literal-phrase overlap, letting a lexically-similar WRONG chunk out-rank the semantically-correct chunk that phrases the same fact differently | SUPERSEDED — see §6g for the corrected, live-derived root cause and landed fix | `traces/golden-trace-thesis093-detail.mjs`, `-thesis093-rawpool.mjs`; provider-payload capture shows model received ONLY the wrong chunk | `evidenceSufficiency.ts` (uncommitted at time of writing — see campaign-log.md) | **FIXED.** The initial hypothesis (a ranking-score tie-break) was refined once the real manual-chat-path numbers were captured directly (not reconstructed from a separate `inspect-retrieval` call) — the actual mechanism is the dynamic-selection early-stop counting UNION coverage across several weak decoy chunks, not a scoring tie. See §6g for the corrected analysis and fix. |
| 13 | THESIS-129/THESIS-131 — `EvidenceResolver.resolveFromOkf`'s OKF-card-first strategy has no fallback to raw hybrid chunks when the OKF cards themselves lossily omit the queried fact | CONFIRMED, root cause refined and FIXED — see §6g | `traces/golden-trace-thesis129-detail.mjs`, `-thesis129-full.mjs`, `-thesis131-full.mjs`, `-okfcards-dump.mjs` | `EvidenceResolver.ts` (uncommitted at time of writing — see campaign-log.md) | **FIXED.** Root cause is more precise than "no fallback": the existing 2026-07-13 salient-term gate DOES exist and DOES sometimes fall through — but it checked the pooled set of ALL selected cards for the salient term, not whether the term co-occurred with the target entity in the SAME card, letting an unrelated entity's card satisfy the gate. See §6g. |
| 14 | THESIS-091 — correct template-example passage not present in the raw retrieval pool for this specific query phrasing (query-dependent recall gap), even though the SAME passage is correctly retrieved under a different query (THESIS-093) | CONFIRMED via raw-pool grep showing zero occurrence of the template phrase for this query | `traces/golden-trace-thesis091-full.mjs`; cross-checked against THESIS-093's raw pool (same passage, present there) | none — not yet fixed | **OPEN, and not yet root-caused past "query-dependent recall gap"** — would need embedding-space inspection to pin further. |
| — | THESIS-072 (3GPP half of a 2-body-standards fact) and THESIS-088 (Open X-Embodiment insufficiency reasoning) | REFUTED as new findings — genuine retrieval-recall misses, no answer-bearing text in raw retrieval pool at all | `traces/golden-trace-thesis-batch2.mjs` | n/a | Matches the pre-existing "genuine retrieval miss" category in `final-report.md` §5 — not investigated further this iteration; safe refusal fired correctly for THESIS-072. |

## 6g. Fixes landed for #12 (evidence-selection early-stop) and #13 (OKF entity-scoped gate) — iteration 11

Both #12 and #13 from §6f were designed, implemented, and live-verified this iteration. Per R1 (evidence before edits), the exact live mechanism for each was re-derived from the REAL manual-chat path (not the earlier `inspect-retrieval`-based reconstruction) before writing any fix code, using temporary trace instrumentation that was fully reverted after use in both cases.

### #13 fix — OKF salient-term gate must be entity-scoped, not pooled

`EvidenceResolver.resolveFromOkf`'s existing distinctive-term gate (added `81517be`, 2026-07-13, for the "working voltage" case) computes a "salient" (rarest) distinctive query term, then checks whether it appears **anywhere across the pooled set of selected OKF cards**. Dumping the full 61-card OKF pack for the thesis document (via a new permanent `entities`/`tags`/`sourceSections`-extended `__e2e__:dump-okf-cards` E2E handler) showed precisely why this pooled check is wrong: for "What model is the visual backbone for the Self-Awareness Tool?" (target entity: "Self-Awareness Tool", salient term: "backbone"), the pool included a card titled "Framework summary" that correctly names the entity but never says its backbone, AND an unrelated "OpenVLA" card that says "backbone" but is describing OpenVLA's OWN architecture, not the Self-Awareness Tool's. The pooled check treated these as jointly satisfying the gate, letting OKF answer from cards that collectively never actually stated the fact.

Fix (`electron/intelligence/context-os/EvidenceResolver.ts`): when the question names a target entity, require the SAME item to carry both the salient term AND the entity (reusing `supportsEntity`, now exported from `evidenceSufficiency.ts` for reuse rather than reimplemented). Falls back to the prior pooled behavior when no entity is named, preserving the exact behavior the 3 pre-existing "working voltage" tests already pin.

**Verified**: new 4th unit test in `EvidenceResolver.test.mjs` reproducing this exact card-pair pattern passes; all 3 pre-existing tests in the same `describe` block pass unchanged. Live-traced THESIS-129/131 fresh — both now answer correctly ("Gemma 3 12B from Google DeepMind", "third-person camera perspective").

### #12 fix — evidence-selection early-stop must require one item with strong (not merely pooled) coverage

Re-instrumented `EvidenceResolver.finalizePack` temporarily to get the REAL composite-ranked candidate list for THESIS-093's question, since the earlier `inspect-retrieval`-based reconstruction (§6f's initial numbers) used a different retrieval call path and did not match the manual-chat path's actual scores. The real ranked order: rank #1 is the WRONG chunk (section 4.2.1, "the second fruit was never interacted with" — describing a DIFFERENT pair of objects, banana/grapes, in an unrelated benchmark) which shares the question's literal 2-word phrase "never interacted with"; ranks #2 and #3 are generic "objects visible" filler chunks from unrelated sections; the CORRECT chunk (section 3.2.1, "an apple and an orange are visible... but no interaction is performed with them") ranks #4 — one slot past `selectSmallestSufficientEvidence`'s `floor = Math.min(3, eligible.length)` (`be7d7e0`, 2026-07-13).

The mechanism: the dynamic early-stop tracks distinctive-term coverage as a **union across ALL selected items**, not per-item. Rank #1 covers `{never, interacted}`; ranks #2/#3 each cover `{objects, visible}` — together these 3 individually-weak, topically-unrelated chunks jointly "cover" all 4 distinctive terms and the floor(3) stop fires immediately, one slot before the genuinely correct chunk at rank #4 (which shares different wording — "no interaction is performed with them" — so it doesn't overlap the same 2 terms rank #1 does).

Fix (`electron/intelligence/context-os/evidenceSufficiency.ts`): require at least one SELECTED item to individually reach strict-majority distinctive-term coverage (reusing the same `covered * 2 > distinctive.length` pattern the codebase already applies in `EvidenceResolver.ts`'s `isAnswerRelevantWithoutEntity`) before the union-based early-stop may fire.

**Verified**: new 6th unit test in `EvidenceSufficiency.test.mjs` reproducing this exact 5-chunk pattern passes; all 5 pre-existing tests in the file pass unchanged (the "dynamic stop... bounded by cap" test's `<=3` assertion tolerates the extra surviving item). Live-traced THESIS-093 THREE separate fresh runs (`golden-trace-thesis093-detail.mjs`) — all 3 now correctly answer "apple and orange", replacing the pre-fix 3-of-3 mix of hallucination/safe-refusal/hallucination observed across identical repeated runs (the pre-fix nondeterminism was itself notable: evidence selection was perfectly deterministic across repeats — always chunks 139/156/126 — but MiniMax-M3 nondeterministically chose to fabricate a plausible-but-wrong answer from chunk 139 vs. correctly refuse, given the SAME incomplete evidence each time; this nondeterminism is now moot since the correct chunk is included).

### Combined regression check

`npx tsc --noEmit` clean. `EvidenceResolver.test.mjs` + `EvidenceSufficiency.test.mjs`: 17/17 pass. Full `electron/intelligence/__tests__/**` suite (75 files): 840/859 pass, the exact same 10 pre-existing failures (`ProfileIdentityBaseline.test.mjs`, `IntelligenceOsE2E.test.mjs` — confirmed unrelated in iteration 7's writeup, still unrelated here) as every prior regression check this campaign. Both fixes live-verified together (not just individually) — THESIS-129/131/093 all still pass after both changes are present simultaneously.

All temporary trace instrumentation added to re-derive ground-truth numbers for both fixes was fully reverted after use in each case — confirmed via `git diff --stat` on `EvidenceResolver.ts` showing only the two intended fix diffs (the entity-scoping change and the import/export changes), no stray `console.log`/`TRACE:` lines.

### Full 140-case benchmark confirmation (iteration 12) — with a self-caught baseline-labeling correction

Launched `dev-run-004-okf-and-093fix` (both fixes landed) and initially compared it against `dev-run-003-okffix`, which I'd labeled a "pre-fix" baseline (121/140 deterministic). The comparison showed only THESIS-093 flipping fail→pass — THESIS-129/131 were ALREADY passing in the supposed "pre-fix" run, which contradicted the golden-trace evidence earlier in this section showing them failing via OKF cards. Investigating the discrepancy (rather than accepting a clean-looking diff at face value) revealed the real sequence: `dev-run-003-okffix` was launched ~37 minutes after landing the OKF fix (#13) but BEFORE the evidence-selection fix (#12) was even designed — so it was mislabeled; it's actually an "OKF-fix-only" snapshot, not a true pre-fix baseline.

Re-derived the correct comparison using `dev-run-002-capfix` (verified genuinely pre-both-fixes by cross-checking its OKF/hybrid routing against `dev-run-001`, an independent, hours-earlier run — both consistently show THESIS-129/131 failing via `okf_exact`/`okf_property`) against `dev-run-004-okf-and-093fix`:

- **Deterministic: 119/140 (85.0%) → 123/140 (87.9%).**
- Flipped fail→pass: THESIS-093, THESIS-129, THESIS-131 (the 3 targeted cases) + **THESIS-042** (unplanned — see below).
- Flipped pass→fail: **none.**

**THESIS-042** is one of the two known instances of H6 (Mercury X1 hardware-spec-table vs. AI-framework-layer entity confusion, `final-report.md` §5) — it now correctly answers "ALOHA" instead of "Mercury X1", apparently resolved as a side effect of the same entity-scoping/strong-match generalization used in both fixes. THESIS-060 was still open at the end of iteration 12; its separate live trace and fix are documented below.

### THESIS-060 H6 root cause and live-verified repair (iteration 14; full benchmark pending)

Source Table 1 says `Control System NVIDIA Jetson Xavier (main), Jetson Nano (aux)`. A fresh real Electron/manual-chat trace proved raw hybrid retrieval already contained that exact row, so this was neither an embedding nor a raw-recall failure. Instead, the OKF-first resolver accepted two decoy cards because broad `processor_or_controller` vocabulary considered ROS/ROS# and an AutoGen agent that “acts as a controller” sufficient proof. The typed `evidence_pack` declared `answer_policy="answer"` but omitted the Table 1 row; Gemini therefore answered ROS plus LLaMA 3.2 7B.

The table-bearing `Technical Specifications` OKF card exists in the pack but was not selected. The minimal repair in `EvidenceResolver` requires candidate evidence for a `processor_or_controller` question that explicitly asks for the main/primary **control system** to contain that exact `control system` field label before it can short-circuit hybrid retrieval. This preserves ordinary controller questions while rejecting generic VR/agent-controller language. The rule applies to OKF and hybrid item property stamping identically.

A new resolver regression uses exactly the observed ROS/AutoGen decoys and requires fallback to a hybrid `Control System NVIDIA Jetson Xavier` chunk. Targeted resolver tests passed **13/13**. In the post-fix live trace, the typed pack contains the literal Table 1 row as a `mode_reference_chunk`, and the generated answer is exactly: “The main control system listed for the Mercury X1 is the NVIDIA Jetson Xavier.” No temp production trace logging was left behind.

**Full regression confirmation:** `dev-run-005-fieldproof` completed all 140 real-provider development cases with 140/140 successful responses, zero timeouts, zero evidence-lineage failures, and zero source-contamination failures. Deterministic score improved from the true `dev-run-004-okf-and-093fix` baseline's **123/140 to 125/140**, with zero pass→fail flips. THESIS-060 flipped fail→pass and its benchmark trace confirms the exact strategy transition from `okf_property` (ROS/AutoGen decoys) to `hybrid_rag` (Table 1 row). THESIS-100 also happened to flip pass but is not attributed to this field-proof repair because it is a previously documented strict-rubric/paraphrase near-miss. This makes THESIS-060 benchmark-verified without claiming the entire benchmark or all H6-class issues are complete.

**THESIS-100** initially looked like a regression (passed in `dev-run-003-okffix`, failed in `dev-run-004`) but investigation showed it ALSO fails in the true pre-fix baseline (`dev-run-002-capfix`) — it was never actually flipped by either fix. It's a stable, pre-existing rubric-rigidity near-miss: the source text says "RGB images... Joint angles for both arms (14-dimensional vector)"; the model's answer (confirmed via 2 fresh live re-traces, not just the one benchmark sample) consistently paraphrases this as "images... 14-dimensional joint states array" — same facts, different wording, correctly failing the strict deterministic substring rubric in every run checked. Not a regression; not touched.

**Process lesson**: a benchmark run started mid-iteration, between landing one fix and designing the next, is neither a clean "before" nor a clean "after" — label run IDs based on the actual git/build state at launch time, and when a comparison result looks inconsistent with independently-established evidence (in this case, the golden traces earlier in this section), investigate the discrepancy rather than accepting the diff at face value. This is now the 3rd time this campaign has caught and corrected an initial finding that didn't hold up under further scrutiny (alongside the H8 correction in §3 and the THESIS-026 scorer-artifact investigation in §6e) — a recurring, healthy pattern worth continuing.

## 7. Diagnostic artifacts

- `traces/golden-trace-refdoc.mjs`, `-refdoc-legacy.mjs`, `-resume-as-refdoc.mjs`, `-jd-fit-legacy.mjs`, `-jd-reqs-repro.mjs`, `-jd-payload-capture.mjs`, `-jd-console-capture.mjs` — all live-path E2E scripts, safe to keep and reuse in Phase 2/3's harness.
- `electron/IntelligenceEngine.ts` currently has 3 temporary `[TRACE:jd-*]` console.log lines added for this investigation (diff below). These are diagnostic-only per R3 — will be used once more to prove the Phase 1 fix, then stripped per R10 before that fix is counted as landed... actually per R3 the tag must be SHOWN FIRING in a benchmark run before the fix counts as landed, so these stay through Phase 1 verification, then get removed in the same commit as the verified fix (not deferred to end-of-campaign R10, since R10 is for the FINAL cleanup pass — but there's no harm keeping them slightly longer if useful for regression-watching in Phase 3; decision: strip them once the fix is proven in this iteration, to keep the checkpoint commit clean).
